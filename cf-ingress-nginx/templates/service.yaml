apiVersion: v1
kind: Service
metadata:
  name: {{ include "cf-ingress-nginx.fullname" . }}
  labels:
    {{- include "cf-ingress-nginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cf-ingress-nginx.selectorLabels" . | nindent 4 }}
