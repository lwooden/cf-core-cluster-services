apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cf-keycloak.fullname" . }}-test-connection"
  labels:
    {{- include "cf-keycloak.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cf-keycloak.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
