apiVersion: v1
kind: Service
metadata:
  name: {{ include "cf-keycloak.fullname" . }}
  labels:
    {{- include "cf-keycloak.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cf-keycloak.selectorLabels" . | nindent 4 }}
