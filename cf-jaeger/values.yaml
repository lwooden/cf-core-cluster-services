cf-jaeger:
  enabled: true
jaeger:
  enabled: true
  image:
    repository: jaegertracing/jaeger
    tag: "2.2.0" # Override the default version
  ingress:
    enabled: true # UI config
    hosts:
      - jaeger.lowsansbox.com
  service:
    pipelines:
      traces:
        receivers: [otlp, jaeger, zipkin]
        processors: [batch]
        exporters: [jaeger_storage_exporter]
